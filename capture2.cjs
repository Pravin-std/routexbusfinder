const http = require('http'); const server = http.createServer((req, res) => { if (req.method === 'POST') { let body = ''; req.on('data', chunk => { body += chunk.toString(); }); req.on('end', () => { console.log('RECEIVED_DATA:', JSON.stringify(JSON.parse(body), null, 2)); res.writeHead(200, { 'Access-Control-Allow-Origin': '*' }); res.end('ok'); }); } else if (req.method === 'OPTIONS') { res.writeHead(200, { 'Access-Control-Allow-Origin': '*', 'Access-Control-Allow-Headers': 'Content-Type' }); res.end(); } }); server.listen(3001, () => console.log('Listening on 3001'));
